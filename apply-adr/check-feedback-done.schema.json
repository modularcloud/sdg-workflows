{
  "type": "object",
  "properties": {
    "done": {
      "description": "true if the feedback explicitly says we can be done AND/OR there are no further non-optional improvement requests. false if the feedback requires any further non-optional improvements before being done — including cases like 'make this important change and then you are done'.",
      "type": "boolean"
    }
  },
  "required": ["done"],
  "additionalProperties": false
}
