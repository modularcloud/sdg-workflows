{
  "type": "object",
  "properties": {
    "has_question": {
      "description": "true only if the text contains an actual pending question that blocks completion of the work (the agent needs an answer before proceeding); false for sign-offs that report work as complete and invite post-hoc corrections",
      "type": "boolean"
    }
  },
  "required": ["has_question"],
  "additionalProperties": false
}
